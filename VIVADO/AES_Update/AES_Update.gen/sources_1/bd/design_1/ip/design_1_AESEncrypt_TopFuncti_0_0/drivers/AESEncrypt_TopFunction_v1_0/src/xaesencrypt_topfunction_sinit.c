// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.1
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xaesencrypt_topfunction.h"

extern XAesencrypt_topfunction_Config XAesencrypt_topfunction_ConfigTable[];

XAesencrypt_topfunction_Config *XAesencrypt_topfunction_LookupConfig(u16 DeviceId) {
	XAesencrypt_topfunction_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XAESENCRYPT_TOPFUNCTION_NUM_INSTANCES; Index++) {
		if (XAesencrypt_topfunction_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XAesencrypt_topfunction_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XAesencrypt_topfunction_Initialize(XAesencrypt_topfunction *InstancePtr, u16 DeviceId) {
	XAesencrypt_topfunction_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XAesencrypt_topfunction_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XAesencrypt_topfunction_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

